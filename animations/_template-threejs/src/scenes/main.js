// ============================================================
//  BOOT — start the engine once every scene file is loaded.
//  (If the animation loads a GLB first, do it here, e.g. loadAvatar().then(boot).)
// ============================================================
boot();
